// 비평가 전용 페이지 — 세 분류(commissions/work/studies)를 모두 방문해야 언락된다.
// 지금은 빈 페이지(추후 내용 채우기).
export default function CriticPage() {
  return (
    <main
      style={{
        position: 'fixed',
        inset: 0,
        background: '#0e0e0e',
      }}
    />
  );
}
